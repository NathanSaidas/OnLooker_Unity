namespace Gem
{
    /// <summary>
    /// Defines the types of input devices accepted by the input manager.
    /// </summary>
    public enum InputDevice
    {
        KEYBOARD,
        MOUSE,
        XBOX_CONTROLLER
    }
}